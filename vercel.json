{
  "buildCommand": "rm -f pnpm-lock.yaml && npm install --save-dev typescript && npm install && npm run build",
  "installCommand": "rm -f pnpm-lock.yaml && npm install --save-dev typescript && npm install",
  "framework": "nextjs"
} 